# Core dependencies for running the bot
python-telegram-bot==20.7
python-dotenv==1.0.1

# Optional: Development dependency for linting
# pylint==3.2.7
# Optional: for SMS reminders
# twilio==8.11.0